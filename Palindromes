# <stdio.h>
#include<string.h>
void main()
{
    char a[20]="MALAYALAM";
    int l=strlen(a)-1;
    for(int i=0,j=l-1;i<j;i++,j--)
    {
        if (a[i]!=a[j])
        printf("It is not palindrome");
        return; 
    }
    printf("It is palindrome");
}
